# Raw corpus

Run `python scripts/download_data.py` from the project root to download the approved text corpus:

- AI.txt
- Machine_Learning.txt
- Database.txt

The script downloads these files from the selected public repository. Do not commit a large raw corpus. If you reuse the data, verify that you are permitted to do so and state its source in your README.
